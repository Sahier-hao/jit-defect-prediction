# JIT 缺陷预测系统 —— 产品需求文档

| 项 | 内容 |
|---|---|
| 团队 | *（待填：组名、成员姓名与分工）* |
| 版本 | v0.2 · Sprint 0 第 1 周（R1 评审后） |
| 日期 | 2026-09-17（v0.1 起草）／2026-09-17（R1 评审） |
| 状态 | **已评审（R1）**，待回填组名/分工与技术栈后定稿 |
| 课程 | 2026-2027-1 软件开发与测试综合实训 |

> ⚠️ 本文档 v0.1 由 AI 起草，其中的**用户画像、优先级、MVP 边界均为假设**。经 **R1 需求评审**（见第 8 节）修订为 v0.2：修正了用户故事优先级统计错误、补齐了 3 条核心故事的验收标准、收缩并明确了 MVP 范围。**组名与成员分工、技术栈选型仍为空白**，须在第 1 周内闭环，否则 Sprint 0 交付物不完整。

---

## 1. 项目背景与问题陈述

### 1.1 问题

软件缺陷发现得越晚，修复成本越高。当前业界的质量保障手段存在一个共同的**时机问题**：

- **传统缺陷预测**是**文件/模块粒度**的——它告诉你"`UserService.java` 这个文件历史上问题多"，但一个文件可能被改过几百次，你仍然不知道**这一次**改动危不危险。
- **静态代码分析工具**（SonarQube、Checkstyle 等）基于**规则**而非历史——它们能发现代码风格和已知反模式，但无法回答"这次提交引入缺陷的概率有多大"，且误报率高，久而久之被开发者忽略。
- **代码评审流程**（GitHub PR Review）本身没有**风险排序**——评审员面对一个 20 个文件的 PR 和另一个 2 个文件的 PR，只能靠直觉决定先看哪个。

结果是：**质量保障的介入时机太晚**（缺陷已进主干），或者**介入时没有优先级依据**（评审精力平均分配）。

### 1.2 解决思路

**即时缺陷预测（Just-in-Time Defect Prediction, JIT）**把预测粒度下沉到**每一次代码提交**：

> 在提交合并进主干之前，预测它引入缺陷的概率，并给出可解释的归因，让代码评审员把有限的审查精力优先投向高风险提交。

这就是**质量保障左移（Shift Left）**——把缺陷拦截在进入主干之前，而不是等到测试阶段甚至线上。

### 1.3 产品价值

对代码评审员：**从"平均用力"变成"重点突破"**，在相同审查工作量下拦截更多缺陷。

对团队：**降低缺陷逃逸率**，减少后期返工成本。

---

## 2. 目标用户与角色

| 角色 | 画像 | 核心诉求 | 使用频率 |
|---|---|---|---|
| **代码评审员**（主要用户） | 团队中负责 Code Review 的开发者，每天要处理多个 PR，审查时间有限 | "告诉我先看哪个提交" | 每天 |
| **算法工程师** | 负责训练、评估、迭代预测模型，关注指标和特征有效性 | "让我快速对比模型、定位特征问题" | 每周 |
| **质量/项目管理者** | 关注项目整体质量走势，需要向上汇报 | "给我看缺陷引入的趋势和分布" | 每周 |
| **系统管理员** | 负责接入新仓库、维护流水线与服务 | "让我低成本接入新项目" | 按需 |

> **起草假设**：课程要求原文强调"指导代码评审员优先审查高风险提交"，因此**代码评审员**是首要用户，界面设计应优先满足其"快速判断"的需求，而非算法工程师的"深度分析"需求。

---

## 3. 竞品与替代方案

| 方案 | 粒度 | 原理 | 局限 |
|---|---|---|---|
| **SonarQube / Checkstyle** | 文件/代码片段 | 静态规则匹配 | 不利用历史数据；误报率高；无法给出风险概率 |
| **传统缺陷预测研究工具** | 文件/模块 | 代码度量 + 机器学习 | 粒度粗；反馈周期长（按版本）；多为学术原型，无工程化界面 |
| **GitHub / GitLab 原生 Review** | 提交 | 无预测 | 仅提供人工评审流程，无风险排序 |
| **商业代码评审平台**（Reviewable 等） | 提交 | 无预测 | 优化评审**体验**而非评审**优先级** |
| **本文系统** | **单次提交** | **历史挖掘 + 机器学习 + 可解释性** | 依赖仓库历史数据质量；冷启动仓库无历史则不可用 |

**差异化定位**：不做又一个静态扫描器，而是**在评审流程中插入一个风险排序层**——用真实开源仓库的历史数据训练模型，对每个提交输出缺陷概率与归因。

---

## 4. MVP 边界

### 4.1 范围内（MVP 必须交付）

- 支持 **Java 开源仓库**接入：MVP 支持多仓库接入与列表管理，但**训练与评估主链路只保证 1 个主仓库**（以 Apache ActiveMQ 为样本）端到端可复现（见 4.3 A-2）
- Git 仓库克隆与提交历史解析
- 缺陷提交识别 + **SZZ 算法**回溯引入缺陷的提交
- **Kamei 14 维**代码变更度量元计算
- **≥2 类（经典机器学习 + 深度学习）共 ≥8 种**预测模型
- **Effort-aware 与 Effort-unaware** 两类评估指标
- 按时间切分的训练/测试集划分
- Web 界面：高风险提交列表（P0）+ 缺陷引入趋势看板（P1，见 4.3 A-1）
- 在线预测接口（单条 + 批量）与预测解释

### 4.2 范围外（明确不做）

| 不做的事 | 原因 |
|---|---|
| 多语言支持（Python/JS/C++ 等） | 先做透 Java 一种，度量元定义与语言强相关 |
| 代码语义特征提取（如 CodeBERT） | 课程标注为「可选」，资源与时间成本高，列为附加功能 |
| 实时 Webhook 自动接入 CI | MVP 以界面单条查询与批量 API 拉取验证价值，实时接入为附加功能（见 4.3 A-3） |
| 多租户与细粒度权限体系 | 单团队使用场景，投入产出比低 |
| 分布式训练与大规模并发 | 数据规模在单机可处理范围 |
| 缺陷自动修复建议 | 超出"预测"定位，属另一个问题域 |
| 模型自动重训练／定时调度／漂移监控 | MVP 无人值守的线上流量，训练为离线批处理，属运维级能力 |

> **范围控制的判断依据**：课程要求明确警告"项目范围过大、无法在各 Sprint 验收会被要求缩减"。上述范围外项若团队认为仍有余力，应作为**附加功能增量**提出，而非塞进 MVP。

### 4.3 R1 评审后的 MVP 调整

> 本节为 2026-09-17 R1 需求评审的产出，评审依据与完整意见见第 8 节。**P0 的范围不再扩张，只允许收缩。**

| # | 调整项 | 调整前 | 调整后 | 理由 | 生效 |
|---|---|---|---|---|---|
| A-1 | 缺陷引入趋势看板（US-5.2.1） | P0 | **P1**，Sprint 2 交付 | 首要用户是评审员，MVP 的对外价值由「高风险提交列表」（US-5.1.1）承担；趋势看板服务质量管理者的汇报视角，可晚一个 Sprint | Sprint 2 |
| A-2 | 仓库数量边界（US-1.1.2／US-1.1.3） | 表述矛盾：4.1 写「单一仓库」，故事要求「仓库列表 + 删除重试」 | MVP **支持多仓库接入与列表管理**，但训练与评估主链路只保证 **1 个主仓库**（ActiveMQ）端到端可复现 | 消除范围歧义；保证 NFR-1 的性能口径与答辩演示均可复现 | 立即 |
| A-3 | 预警介入方式 | 价值主张写「合并进主干之前预警」，但 MVP 不做 CI/Webhook | 明确 MVP 的介入方式为**界面单条查询 + 批量 API 拉取**；实时 CI 接入列为附加功能；价值主张表述限定为「评审前置预警」 | 避免价值主张与交付能力脱节，答辩时被质疑"并未接入评审流程" | 立即 |
| A-4 | 附加功能排期 | 统一排在「Sprint 3」 | 只允许放在 **Sprint 2 末**，或作为 Sprint 3 的**非验收增量** | 课程将 Sprint 3 定义为「测试与验证」阶段，不是新功能开发期 | 立即 |

**MVP 主链路（Sprint 1 末必须可演示）**：

`US-1.1.1 接入仓库 → US-1.2.1 解析提交 → US-1.3.2 SZZ 回溯 → US-2.1.1 计算 Kamei 14 维 → US-2.2.1 时间切分 → US-3.1.1 训练 1 个基线模型 → US-3.3.4 登记模型 → US-4.1.1 单条预测 → US-4.2.1 特征归因 → US-5.1.1 风险列表`

> 课程要求「MVP 包含至少 1 条从界面/入口到业务/数据层的完整流程」——上述链路即为该条流程的证据，Sprint 1 演示时必须演示完整链路而非分段截图。

---

## 5. 用户故事

**优先级定义**：`P0` = MVP 必须交付，缺失则演示不成立｜`P1` = 应该交付，影响产品完整性｜`P2` = 可选增强。

### Epic 1：数据采集与仓库解析

> 业务目标：把真实开源仓库的提交历史变成可训练的、带缺陷标签的结构化数据。

#### Feature 1.1 仓库接入

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-1.1.1 | 作为**算法工程师**，我想要**通过界面填入 Git 仓库地址**，以便系统自动拉取代码历史，而不用手工执行命令 | P0 |
| US-1.1.2 | 作为**算法工程师**，我想要**查看已接入仓库的列表及其状态**（克隆中／解析中／已就绪／失败），以便掌握数据准备进度 | P0 |
| US-1.1.3 | 作为**系统管理员**，我想要**对克隆失败的仓库重试或删除**，以便清理无效数据、释放存储 | P1 |

**US-1.1.1 验收标准**
- WHEN 用户提交一个合法的 Git 仓库地址，THEN 系统创建接入任务并返回任务 ID
- WHEN 用户提交非法地址或私有仓库且无凭据，THEN 系统返回明确错误信息，且不创建脏数据
- WHEN 仓库体积超过设定上限，THEN 系统拒绝并提示上限值

#### Feature 1.2 提交历史解析

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-1.2.1 | 作为**算法工程师**，我想要**系统解析每个提交的元数据**（作者、时间、变更文件、增删行数），以便作为特征计算的输入 | P0 |
| US-1.2.2 | 作为**算法工程师**，我想要**系统自动过滤合并提交与纯格式变更**（如仅改空白、注释），以便减少噪声样本、提升标签质量 | P1 |

**US-1.2.1 验收标准**
- WHEN 解析一个含 N 个提交的仓库，THEN 数据库中产生 N 条提交记录（扣除被过滤项）
- WHEN 某提交的变更文件无法解析（如二进制文件），THEN 跳过该文件且记录警告，不中断整体解析

#### Feature 1.3 缺陷标注（SZZ 算法）

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-1.3.1 | 作为**算法工程师**，我想要**系统自动识别缺陷修复提交**（依据 commit message 中的 issue 编号或缺陷关键词），以便构建初始标签 | P0 |
| US-1.3.2 | 作为**算法工程师**，我想要**系统执行 SZZ 算法回溯出真正引入缺陷的提交**，以便得到训练所需的标签 | P0 |
| US-1.3.3 | 作为**算法工程师**，我想要**查看标注结果统计**（缺陷提交占比、回溯成功/失败数、无法归因的修复提交），以便判断数据质量是否可训练 | P1 |

**US-1.3.2 验收标准**
- WHEN 给定一个已识别的缺陷修复提交，THEN 系统输出被修改的代码行及其对应的引入提交列表
- WHEN 引入提交早于仓库起始或无法定位，THEN 该样本标记为"无法归因"而非错误丢弃，并计入统计

#### Feature 1.4 数据持久化

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-1.4.1 | 作为**算法工程师**，我想要**解析结果持久化到数据库**，以便重复训练时无需重新克隆和解析 | P0 |
| US-1.4.2 | 作为**系统管理员**，我想要**在数据异常时重建指定仓库的数据集**，以便从损坏状态恢复 | P2 |

---

### Epic 2：特征工程与样本构建

> 业务目标：把原始提交转换成模型可消费的特征向量与标签。

#### Feature 2.1 变更度量元

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-2.1.1 | 作为**算法工程师**，我想要**系统自动计算 Kamei 14 维代码变更度量元**，以便作为模型的标准输入特征 | P0 |
| US-2.1.2 | 作为**算法工程师**，我想要**查看单个提交的 14 维特征明细**，以便核查计算逻辑是否正确 | P1 |
| US-2.1.3 | 作为**算法工程师**，我想要**以插件方式新增自定义度量元**，以便在不改动核心代码的前提下扩展特征集 | P2 |

**US-2.1.1 验收标准**
- WHEN 对一个已解析的提交计算特征，THEN 输出的 14 个维度全部有值，缺失值有明确的填充策略与标记
- WHEN 同一提交重复计算，THEN 结果一致（计算具备确定性）

> **课程对齐**：Kamei 14 维度量元出自 Kamei et al., *A Large-Scale Empirical Study of Just-in-Time Quality Assurance*, TSE 39(6), 2013——课程要求原文引用的文献。

#### Feature 2.2 样本集构建

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-2.2.1 | 作为**算法工程师**，我想要**按时间顺序切分训练集与测试集**，以便模拟"用历史预测未来"的真实场景 | P0 |
| US-2.2.2 | 作为**算法工程师**，我想要**剔除特征不完整的样本**，以便保证模型输入质量，并知道剔除了多少 | P1 |
| US-2.2.3 | 作为**算法工程师**，我想要**查看数据集的类别分布**（缺陷／非缺陷比例），以便判断是否需要处理类别不平衡 | P1 |

**US-2.2.1 验收标准**
- WHEN 执行切分，THEN 训练集的所有提交时间**早于**测试集的任何提交（无时间穿越）
- WHEN 数据量不足以切分，THEN 系统报错并提示最小样本量要求

---

### Epic 3：模型训练与评估

> 业务目标：训练多类模型并给出可信的对比结论，支撑"选哪个模型上线"的决策。

#### Feature 3.1 经典机器学习模型

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-3.1.1 | 作为**算法工程师**，我想要**一键训练多种经典模型**（逻辑回归、朴素贝叶斯、决策树、随机森林、XGBoost 等，合计不少于 5 种），以便横向对比效果 | P0 |
| US-3.1.2 | 作为**算法工程师**，我想要**调整超参数并重新训练**，以便针对本项目数据优化性能 | P1 |
| US-3.1.3 | 作为**算法工程师**，我想要**查看训练过程日志与耗时**，以便定位慢环节和失败原因 | P2 |

#### Feature 3.2 深度学习模型

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-3.2.1 | 作为**算法工程师**，我想要**训练基于提交变更的深度学习模型**（如 MLP／CNN／LSTM 等，不少于 3 种），以便与经典方法对比 | P0 |
| US-3.2.2 | 作为**算法工程师**，我想要**指定训练轮数与批大小**，以便在可接受时间内完成训练 | P1 |

> **课程对齐**：课程硬性要求"至少 2 类（经典机器学习和深度学习）不少于 8 种预测模型"，故经典 5 种 + 深度 3 种为最低配置，团队可按精力上调。

#### Feature 3.3 评估与对比

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-3.3.1 | 作为**算法工程师**，我想要**查看每个模型的 Effort-unaware 指标**（Precision／Recall／F1／AUC），以便了解整体预测能力 | P0 |
| US-3.3.2 | 作为**算法工程师**，我想要**查看 Effort-aware 指标**（如 Popt、Recall@前 20% 工作量），以便评估在真实审查场景下的实际收益 | P0 |
| US-3.3.3 | 作为**算法工程师**，我想要**查看模型对比表与对比图**，以便快速选出上线模型 | P1 |
| US-3.3.4 | 作为**算法工程师**，我想要**将选定模型保存并登记版本**（含训练数据范围、参数、指标），以便上线使用且结果可复现 | P0 |

**US-3.3.2 验收标准**
- WHEN 生成评估报告，THEN 同时包含 Effort-unaware 与 Effort-aware 两组指标，缺一不可
- WHEN 不同模型使用不同测试集，THEN 系统禁止对比（防止结论失效）

> **课程对齐**：课程要求"评估指标（Effort-unaware 和 Effort-aware 两类）"。两类指标回答不同问题：前者问"预测准不准"，后者问"按风险排序后，同样审查 20% 的提交能拦下多少缺陷"——**后者才是本产品价值的直接证据**。

---

### Epic 4：预测与解释

> 业务目标：把训练好的模型变成评审员能用的判断依据。

#### Feature 4.1 在线预测

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-4.1.1 | 作为**代码评审员**，我想要**输入或选择一个提交并获得缺陷概率**，以便判断是否优先审查 | P0 |
| US-4.1.2 | 作为**系统管理员**，我想要**通过 API 批量提交提交列表获取预测结果**，以便集成到 CI 流程 | P1 |
| US-4.1.3 | 作为**代码评审员**，我想要**看到每个提交的风险等级**（高／中／低），而不只是一个数字，以便快速决策 | P1 |

**US-4.1.1 验收标准**（R1 评审补充）
- WHEN 用户在界面选择一个已解析仓库中的提交，THEN 返回该提交的缺陷概率（0-1 区间）与所用模型版本号
- WHEN 该提交不在已解析数据范围内（如尚未解析的新提交），THEN 返回明确提示"该提交无可用特征"，不返回编造的默认概率
- WHEN 所选模型尚未训练或已失效，THEN 界面提示"模型不可用"并引导至模型登记列表，而非静默失败

#### Feature 4.2 可解释性

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-4.2.1 | 作为**代码评审员**，我想要**看到影响本次预测的关键特征及其方向**（如"变更文件数偏多"推高风险），以便理解风险来源、有针对性地审查 | P0 |
| US-4.2.2 | 作为**算法工程师**，我想要**看到全局特征重要度排序**，以便优化特征工程 | P1 |

**US-4.2.1 验收标准**
- WHEN 返回任一预测结果，THEN 同时返回至少 3 个贡献最大的特征及其影响方向
- WHEN 模型不支持原生解释（如深度学习），THEN 使用模型无关的解释方法（如 SHAP）并标注所用方法

---

### Epic 5：可视化前端

> 业务目标：让不同角色在 10 秒内看到自己关心的信息。

#### Feature 5.1 风险列表

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-5.1.1 | 作为**代码评审员**，我想要**看到按缺陷概率降序排列的提交列表**，以便优先审查高风险提交 | P0 |
| US-5.1.2 | 作为**代码评审员**，我想要**按仓库、时间范围、概率阈值筛选列表**，以便聚焦当下关心的范围 | P1 |
| US-5.1.3 | 作为**代码评审员**，我想要**点击列表项查看该提交的详情**（变更文件、特征、归因），以便决定是否深入审查 | P1 |

**US-5.1.1 验收标准**（R1 评审补充）
- WHEN 打开风险列表，THEN 列表按缺陷概率降序排列，并显示每条提交的概率、风险等级与提交时间
- WHEN 列表数据量超过单页容量，THEN 分页或滚动加载，且排序在全局范围成立（不是仅对当前页排序）
- WHEN 仓库尚未完成解析或尚无训练好的模型，THEN 页面显示空状态说明与下一步指引，不显示空白表格

#### Feature 5.2 趋势看板

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-5.2.1 | 作为**质量管理者**，我想要**看到缺陷引入数量和比例随时间的变化趋势**，以便评估质量走势 | P1（R1 由 P0 降级，见 4.3 A-1） |
| US-5.2.2 | 作为**质量管理者**，我想要**看到缺陷按模块／目录的分布**，以便定位问题集中的区域 | P1 |
| US-5.2.3 | 作为**质量管理者**，我想要**看到模型性能指标概览**，以便了解预测结果本身的可信度 | P2 |
| US-5.2.4 | 作为**代码评审员**，我想要**看到高风险提交的贡献者分布**，以便安排评审资源 | P2 |

---

### Epic 6：工程化

> 业务目标：让系统可持续演进，且开发过程本身留下可核验的证据。

#### Feature 6.1 协作规范与过程留痕

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-6.1.1 | 作为**团队成员**，我想要**按分支规范开发并通过 PR 合并（合并前经非作者审阅）**，以便留下可核验的个人贡献记录 | P0 |
| US-6.1.2 | 作为**团队成员**，我想要**需求变更时同步更新 OpenSpec 记录**，以便规格与实现始终一致 | P0 |

**US-6.1.1 验收标准**（R1 评审补充）
- WHEN 任一功能分支提交 PR，THEN PR 描述中包含关联的 OpenSpec change 与用户故事 ID，且合并前存在至少一位非作者的 Approve 记录
- WHEN 作者本人是唯一评审者或存在未解决的变更请求，THEN 该 PR 不得合并（分支保护或人工检查）
- WHEN 需求发生变更，THEN 对应 OpenSpec change 同步更新，且 PR 中可追溯到该次变更

#### Feature 6.2 CI/CD

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-6.2.1 | 作为**开发者**，我想要**在提交或 PR 时自动执行构建与测试**，以便及早发现问题 | P1 |
| US-6.2.2 | 作为**开发者**，我想要**在 CI 中执行 OpenSpec 规范校验**，以便结构不合格的规范被拦截 | P2 |

#### Feature 6.3 服务部署

| ID | 用户故事 | 优先级 |
|---|---|---|
| US-6.3.1 | 作为**系统管理员**，我想要**通过容器化方式部署整个系统**，以便一键启动全部服务 | P2 |

---

### 用户故事统计

| Epic | Feature 数 | Story 数 | P0 | P1 | P2 |
|---|---|---|---|---|---|
| 1 数据采集与仓库解析 | 4 | 10 | 6 | 3 | 1 |
| 2 特征工程与样本构建 | 2 | 6 | 2 | 3 | 1 |
| 3 模型训练与评估 | 3 | 9 | 5 | 3 | 1 |
| 4 预测与解释 | 2 | 5 | 2 | 3 | 0 |
| 5 可视化前端 | 2 | 7 | 1 | 4 | 2 |
| 6 工程化 | 3 | 5 | 2 | 1 | 2 |
| **合计** | **16** | **42** | **18** | **17** | **7** |

> **R1 评审修正（R1-01）**：v0.1 的统计表与正文不符——正文实际为 **P0 19／P1 16／P2 7**，表中却写作 21／15／6，且 Epic 2、Epic 3 的分行计数也有误。上表已按正文逐条重算，并计入 4.3 的范围调整（US-5.2.1 由 P0 降为 P1），最终为 **P0 18／P1 17／P2 7**。**后续任何优先级改动都必须同步更新本表**，避免同一问题复发。

> 课程要求"至少 20 个彼此关联的核心用户故事"。**42 条**是有余量的版本——团队若认为工作量过大，建议优先砍 **P2 全部（7 条）**，仍余 35 条。

**验收标准覆盖情况（R1 评审新增统计）**：42 条故事中 **10 条**已写明验收标准（全部为 P0），P0 覆盖率为 **10/18**。课程明确要求"每个用户故事应确定其优先级、验收标准"，剩余 **8 条 P0**（US-1.1.2、US-1.3.1、US-1.4.1、US-3.1.1、US-3.2.1、US-3.3.1、US-3.3.4、US-6.1.2）须在第 2 周补齐，清单见 8.4。

---

## 6. 非功能需求

课程要求"至少选择并落实 2 项与项目相关的质量需求"。以下为建议的 4 项，**团队须从中选定不少于 2 项，并在 Sprint 3 给出验证证据**。

| # | 质量属性 | 具体指标（待团队确认） | 验证方式 |
|---|---|---|---|
| NFR-1 | **性能** | 单次预测接口响应时间 P95 < 2 秒（**口径：不含解释计算**）；解析 1 万提交规模的仓库 < 30 分钟 | 压测脚本 + 计时日志 |
| NFR-2 | **可解释性** | 100% 的预测结果附带至少 3 个特征的归因说明 | 接口返回字段校验 |
| NFR-3 | **可靠性** | 训练任务失败后可重试且不产生脏数据；服务重启后模型可用 | 故障注入测试 |
| NFR-4 | **易用性** | 新用户从打开界面到完成首次预测 ≤ 5 分钟，无需阅读文档 | 可用性测试（找未参与开发的同学试用并计时） |

> **R1 评审结论（Q5 闭环）**：**正式选定 NFR-1 + NFR-2**，NFR-4 作为第三项在第 3 周补充证据。理由：NFR-1 有客观数据、易出证据；NFR-2 直接支撑产品核心价值（"可解释"是本系统区别于纯黑盒预测的关键）。**不选"安全性"**：MVP 为单机/内网单团队使用、不托管他人代码与账号数据，按 4.2 已排除多租户与权限体系，选它无法产出有说服力的验证证据。
>
> **R1 评审澄清（R1-08）**：NFR-1 与 NFR-2 存在时序冲突——SHAP 等模型无关解释方法对深度学习模型的单次计算耗时可超过 2 秒。为避免自相矛盾的验收，约定：**NFR-1 的计时不含解释计算**；解释结果允许预计算或缓存，**缓存命中时包含解释在内的整体响应仍须 < 2 秒**，缓存未命中时以异步或"计算中"状态返回，不得超时失败。该口径同时写入 Sprint 2 的设计文档。

---

## 7. 待确认问题（R1 评审后状态跟踪）

状态：`✅ 已闭环`｜`🔶 待确认/进行中`｜`⛔ 未闭环（阻塞 Sprint 0 交付）`

| # | 问题 | 评审结论／建议 | 状态 | 责任 | 截止 |
|---|---|---|---|---|---|
| Q1 | **组名与成员分工**：谁是 PO、谁是 Scrum Master？ | 评审无法代决。须在第一次课上确定并**回填本文档头部表格**；分工同时写入协作约定与看板 | ⛔ | 全组 | 第 1 周 |
| Q2 | **训练用仓库选哪个？** | 采纳 **Apache ActiveMQ** 为主仓库（体积适中、issue 规范、Kamei 研究常用）；须实测克隆体积、解析耗时与磁盘占用，作为 NFR-1 的实际测量对象 | 🔶 | 算法负责人 | 第 2 周 |
| Q3 | **技术栈选型** | 建议后端 **FastAPI**（与 SZZ/Kamei 及 scikit-learn、PyTorch 生态同栈，避免跨语言调用）＋前端 **Vue**＋**PostgreSQL**；若团队 Java 熟练度更高，选 Spring Boot 但需额外评估"Python 模型服务如何被调用"。**需团队确认后写入设计文档** | 🔶 | 架构负责人 | 第 2 周 |
| Q4 | **P0 的 Story 是否全部塞进 Sprint 1-3？** | 已闭环：原表 P0 计数有误（正文实为 19 条），且经 4.3 调整后为 **18 条**。**Sprint 1 只承诺 4.3 的主链路**，其余按 8.5 的 Sprint 映射推进 | ✅ | PO | — |
| Q5 | **非功能需求选哪 2-3 项？** | 已闭环：选定 **NFR-1 + NFR-2**，NFR-4 作为第三项；不选安全性的理由已写入第 6 节 | ✅ | PO | — |
| Q6 | **前端用户侧重谁？** | 已闭环：维持**评审员优先**；质量管理者视角的趋势看板（US-5.2.1）因此由 P0 降为 P1 | ✅ | PO | — |
| Q7 | **42 条 Story 的粒度是否合适？** | 粒度偏粗的 P0 故事（如 US-1.1.1、US-3.1.1）须在 Sprint 计划时拆分为 **≤ 2 人日**的可估算任务，并作为 OpenSpec `tasks` 落库；拆分后不得改变原故事的验收标准 | 🔶 | Scrum Master | 第 3 周 |

---

## 8. 需求评审记录（R1 评审轮）

### 8.1 评审信息

| 项 | 内容 |
|---|---|
| 评审轮次 | R1（Sprint 0 第 1 周） |
| 评审对象 | 本文档 v0.1（AI 起草稿） |
| 评审日期 | 2026-09-17 |
| 评审执行 | AI 辅助走查 + 统计量复核（由团队发起的评审轮次）；**结论须经团队确认后生效** |
| 评审依据 | 课程项目要求（2026-2027-1 软件开发与测试综合实训）；Kamei et al., *A Large-Scale Empirical Study of Just-in-Time Quality Assurance*, TSE 39(6), 2013；团队可投入的人力与 Sprint 0-3 周期 |
| 评审方法 | 逐节走查 + 与课程硬性要求逐条对照（见 8.3）+ 统计量重算（用户故事优先级分布、验收标准覆盖率） |
| 评审结论 | **有条件通过**：无阻塞性需求缺失，但 MVP 范围需收缩、验收标准需补齐、统计数字需修正。修订后的 v0.2 经团队确认组名/分工与技术栈后即可定稿为 v1.0 |

### 8.2 评审发现与处理

严重度：`B` = 阻塞（不解决则影响验收）｜`M` = 重要（影响交付质量）｜`S` = 建议

| ID | 严重度 | 位置 | 问题 | 处理结论 |
|---|---|---|---|---|
| R1-01 | B | 第 5 节「用户故事统计」 | 统计表与正文不符：正文实际为 P0 19／P1 16／P2 7，表中写作 21／15／6，且 Epic 2、Epic 3 的分行计数亦错 | **已修正**为按正文重算，并计入 4.3 调整后更新为 **18／17／7**（见第 5 节） |
| R1-02 | B | 第 5 节 | 课程要求"每个用户故事应确定其优先级、验收标准"，但 42 条中仅 7 条有验收标准（17%），P0 中仅 7/19 | **已补** 3 条核心故事验收标准（US-4.1.1、US-5.1.1、US-6.1.1）；其余 8 条 P0 列入 8.4 待办 |
| R1-03 | B | 4.1 vs US-1.1.2／US-1.1.3 | 范围写"支持**单一** Java 仓库接入"，故事却要求"已接入仓库**列表**""克隆失败重试或删除"——单仓库与多仓库边界自相矛盾 | **已明确**（4.3 A-2）：MVP 支持多仓库接入与列表管理，但训练/评估主链路只保证 **1 个主仓库**端到端可复现 |
| R1-04 | M | 1.2 vs 4.1／4.2 | 价值主张是"提交**合并进主干之前**预警"，但 MVP 不含 CI/Webhook 接入，实际只能人工查询；答辩时价值主张与交付能力存在缺口 | **已补**入 4.3 A-3：MVP 介入方式明确为「界面单条查询 + 批量 API 拉取」，价值主张限定为"评审前置预警"；实时接入列为附加功能 |
| R1-05 | M | Feature 3.3 | Effort-aware 指标只写了 Popt、Recall@20%，未定义 **effort 代理变量**与基线，指标不可复现、不可验收 | **待补**：第 2 周在设计文档中固定 effort 代理量（变更行数或变更文件数）、@比例与对比基线，并回写第 5 节验收标准 |
| R1-06 | M | US-2.2.1 | 只写"按时间切分"，未给切分点与比例，训练/测试集不可复现 | **待补**：固定为按提交时间升序的**前 70%／后 30%**，切分点与样本量写入模型登记信息（US-3.3.4） |
| R1-07 | M | Feature 1.3 | SZZ 依赖 issue 关键词启发式；"无法归因"样本只写"计入统计"，未说明是否进入训练集 | **待补**：明确无法归因样本**不进入训练集**，但在数据质量报告中单独统计并给出占比 |
| R1-08 | M | 第 6 节 NFR-1／NFR-2 | SHAP 解释与"单次预测 P95 < 2 秒"可能冲突（深度模型解释耗时可超 2 秒） | **已澄清**：NFR-1 计时口径不含解释计算；解释允许预计算/缓存，缓存命中时含解释的整体响应仍 < 2 秒（见第 6 节） |
| R1-09 | M | Feature 6.2／6.3、附 A | 文档把附加功能统一排在「Sprint 3」，而课程将 Sprint 3 定义为**测试与验证**阶段，定位冲突 | **已修正**（4.3 A-4）：附加功能只允许在 Sprint 2 末或作为 Sprint 3 的非验收增量 |
| R1-10 | M | 4.1、Feature 1.1 | "冷启动仓库不可用"只出现在竞品局限中，MVP 无**最小数据量门槛**，可能出现样本不足导致模型无法训练 | **待补**：接入前校验最小规模（建议 ≥ 1000 个提交且缺陷提交 ≥ 50），低于阈值拒绝并提示原因 |
| R1-11 | S | 全文 | 课程原文写作 "Effort-**unware**"，文档写作 "Effort-**unaware**" | **保留**文档的正确拼写，并在 8.3 注明为课程原文笔误，含义一致，避免评审老师误解 |
| R1-12 | S | Feature 3.1／3.2 | 8 个模型重复训练的时间成本未约束，Sprint 2 有被训练耗时吃掉的风险 | **待补**：约束为「统一复用同一份特征矩阵、单模型训练 ≤ 15 分钟、超参搜索预算固定」，写入设计文档 |
| R1-13 | S | Feature 3.3 | 类别不平衡与随机性未约束，模型对比结论可能不稳定 | **待补**：报告缺陷样本占比；每个模型固定随机种子并重复 ≥ 3 次，报告均值与标准差 |
| R1-14 | S | Feature 6.3／4.2 | 课程中的"缺陷管理工具"要求在本文档中没有任何落点 | **待补**：在 Sprint 0 选定 Issue/缺陷管理方式（GitHub Issues 或看板工具），并规定缺陷记录须含现象、复现条件、优先级、处理状态与关联提交（见 8.4） |

### 8.3 课程硬性要求对齐校核

> 依据：`docs/2026-2027-1 软件开发与测试综合实训 项目要求.pdf`。`✅` = 已覆盖｜`🔶` = 覆盖但口径待补｜`⛔` = 尚未开始或缺失

| # | 课程要求 | 文档落点 | 结论 |
|---|---|---|---|
| C-1 | 自由组队 3~4 人，保留可核验的个人贡献记录 | 头部表格（待填）、US-6.1.1 | ⛔ 待填组名与分工 |
| C-2 | 面向明确用户与真实场景，不得复刻产品或只做教程式 CRUD | 1.1／1.2／第 2 节 | ✅ |
| C-3 | 须包含可供目标用户使用的界面或交互入口，以及可维护的业务/数据处理层 | 4.1、Epic 5 | ✅ |
| C-4 | 使用 Scrum，每个 Sprint 交付可演示、可运行的产品增量 | 8.5 Sprint 映射 | 🔶 原文档无 Sprint 映射，已补 |
| C-5 | 清晰的 MVP 与可选附加功能 | 4.1／4.2／4.3 | ✅ |
| C-6 | ≥ 20 个彼此关联的核心用户故事，形成完整业务闭环 | 第 5 节（42 条，Epic 1→6） | ✅ |
| C-7 | 功能需求 + 非功能需求，非功能需求至少落实 2 项 | 第 6 节（选定 NFR-1＋NFR-2） | ✅ |
| C-8 | 项目启动期初始化 OpenSpec 并提交版本控制 | 附：下一步（第 3 周） | ⛔ 未开始 |
| C-9 | 项目管理看板，任务对应到 Sprint 与 OpenSpec change/task | 附：下一步（第 3 周） | ⛔ 未开始 |
| C-10 | 使用缺陷管理工具记录缺陷 | 本文档原无落点 | ⛔ 缺口，见 R1-14 |
| C-11 | 使用 PR 协作，合并前至少一位非作者审阅，审阅意见与修改记录留存 Git | US-6.1.1（含 R1 补充的验收标准） | ✅ 本次 v0.2 变更将通过 `feat/requirements-review` 分支的 PR 审阅首次落地该机制 |
| C-12 | 实现 Kamei 14 维代码变更度量元 | US-2.1.1 | ✅ |
| C-13 | 至少 2 类（经典机器学习 + 深度学习）共不少于 8 种预测模型 | US-3.1.1（≥ 5）+ US-3.2.1（≥ 3） | ✅ |
| C-14 | 评估指标须包含 Effort-unaware 与 effort-aware 两类 | US-3.3.1、US-3.3.2 | 🔶 口径待补（R1-05） |
| C-15 | 训练集／测试集划分 | US-2.2.1 | 🔶 比例待补（R1-06） |
| C-16 | 预测与解释模块（缺陷概率、缺陷归因/解释） | Epic 4 | ✅ |
| C-17 | 可视化前端（风险列表、缺陷引入趋势看板等） | Epic 5 | ✅ |
| C-18 | 工程化（Git 分支规范 + 代码评审；CI/CD、插件化等） | Epic 6 | ✅ |
| C-19 | Sprint 3 完成至少两项已验证的非功能需求测试 | 第 6 节 | ✅ |
| C-20 | 技术架构（建议三层/微服务）与数据库表结构设计 | 附：下一步（第 2 周） | ⛔ 未开始，属 Sprint 0 交付物 |

> **校核结论**：8 项硬性功能/流程要求（C-6、C-7、C-12、C-13、C-16、C-17、C-18、C-19）在需求层面全部覆盖；**风险集中在过程类交付物（C-1、C-8、C-9、C-10、C-20）**——它们不写进需求文档就会被忘记，因此已全部排入 8.4 与 8.5。

### 8.4 评审后待办事项（含 P0 验收标准补齐清单）

| # | 事项 | 关联 | 责任 | 截止 | 验收方式 |
|---|---|---|---|---|---|
| T-1 | 回填组名、成员姓名与 PO／Scrum Master 分工 | Q1、C-1 | 全组 | 第 1 周 | 本文档头部表格不再有"待填" |
| T-2 | 补齐 8 条 P0 故事的验收标准：US-1.1.2、US-1.3.1、US-1.4.1、US-3.1.1、US-3.2.1、US-3.3.1、US-3.3.4、US-6.1.2 | R1-02 | 各 Feature 负责人 | 第 2 周 | 每条含正常场景 + 至少 1 个异常/边界场景 |
| T-3 | 固定 effort 代理变量、@比例与对比基线 | R1-05、C-14 | 算法负责人 | 第 2 周 | 设计文档 + 第 5 节验收标准同步更新 |
| T-4 | 固定训练/测试切分比例（前 70%／后 30%）并写入模型登记字段 | R1-06、C-15 | 算法负责人 | 第 2 周 | 两次运行结果可复现 |
| T-5 | 明确"无法归因"样本不进入训练集并单独统计 | R1-07 | 算法负责人 | 第 2 周 | 数据质量报告含占比 |
| T-6 | 定义最小数据量门槛（≥ 1000 提交且缺陷提交 ≥ 50） | R1-10 | 数据负责人 | 第 2 周 | 接入校验可拒绝不合格仓库 |
| T-7 | 约定单模型训练时间上限与超参搜索预算 | R1-12 | 算法负责人 | 第 2 周 | 8 模型全量训练在一个 Sprint 内完成 |
| T-8 | 确定技术栈并写入设计文档 | Q3、C-20 | 架构负责人 | 第 2 周 | 第 2 周交付物评审通过 |
| T-9 | 选定缺陷管理工具与缺陷记录字段模板 | R1-14、C-10 | Scrum Master | 第 3 周 | 至少 1 条真实缺陷按模板登记 |
| T-10 | OpenSpec 初始化（proposal + 核心能力规格 + tasks）与看板建立 | C-8、C-9 | Scrum Master | 第 3 周 | `openspec/` 提交入库，看板任务可追溯到 change |

### 8.5 Sprint 映射（MVP 主链路与增量）

| 阶段 | 周期 | 本 Sprint 的目标增量 | 对应用户故事 |
|---|---|---|---|
| Sprint 0 | 第 1-3 周 | 需求定稿（本文档）、架构与数据库表结构、低保真原型、OpenSpec 初始化、看板与缺陷管理工具就位 | — |
| Sprint 1 | 第 4-6 周 | **打通一条完整主链路**：接入仓库 → 解析提交 → SZZ 标注 → Kamei 14 维 → 时间切分 → 训练 1 个基线模型 → 登记模型 → 单条预测 → 特征归因 → 风险列表 | US-1.1.1、1.2.1、1.3.1、1.3.2、1.4.1、2.1.1、2.2.1、3.1.1（首模型）、3.3.4、4.1.1、4.2.1、5.1.1 |
| Sprint 2 | 第 7-9 周 | 补齐模型广度与评估完整性：8 种模型全部可训练、Effort-aware 与 Effort-unaware 双指标、模型对比、趋势看板、批量 API；附加功能（语义特征、容器化）在此阶段末投入 | US-3.1.1、3.2.1、3.3.1、3.3.2、3.3.3、4.1.2、4.1.3、5.2.1、5.2.2 |
| Sprint 3 | 第 10-14 周 | **测试与验证**：单元/集成/端到端测试、缺陷修复、≥ 2 项非功能需求的验证证据、文档定稿 | US-1.1.2、1.1.3、1.3.3、2.2.2、2.2.3、5.1.2、5.1.3、6.1.1、6.1.2 |
| Final Release | 第 15-16 周 | 最终可运行系统、测试报告、OpenSpec 归档、答辩材料 | — |

> Sprint 3 的定位遵循课程要求（测试与验证），**不在该阶段承诺新功能**；P2 故事如未在 Sprint 2 完成，一律顺延为"未完成项"并在周报中如实记录，而不是挤占验收与测试时间。

### 8.6 遗留风险与已知限制

| # | 风险／限制 | 影响 | 应对 |
|---|---|---|---|
| RK-1 | MVP 无 CI/Webhook 实时接入，预警依赖人工触发 | 价值主张弱于"自动拦截" | 已在 4.3 A-3 限定表述，并在演示中明确说明为附加功能 |
| RK-2 | 无鉴权与多租户，不做安全性非功能验证 | 无法回答"安全类非功能需求" | 已在第 6 节说明选型理由；演示限定为单机/内网 |
| RK-3 | 预测能力上限由开源仓库历史数据与 issue 规范决定 | 指标可能低于论文报告值 | 在报告中如实给出数据质量统计与基线对比，不夸大结论 |
| RK-4 | 深度学习模型样本量与算力受限 | 深度模型可能不如经典模型 | 这本身即是可汇报的实验结论；按 R1-13 报告均值与标准差 |
| RK-5 | 8 个模型的训练与调参时间成本高 | Sprint 2 有超期风险 | 按 T-7 约束训练预算；必要时下调超参搜索范围而非削减模型数量（模型数量是课程硬性要求） |

---

## 附 A：下一步

| 时间 | 事项 |
|---|---|
| 第 1 周（本周） | ✅ R1 需求评审完成（第 8 节）→ 回填组名与分工（T-1）、闭环 Q1/Q3 → 本文档定稿为 v1.0 |
| 第 2 周 | 技术架构设计、数据库表结构、核心流程图与时序图、低保真原型；补齐 8 条 P0 验收标准（T-2）并固定 effort 口径、切分比例、数据量门槛（T-3~T-7） |
| 第 3 周 | OpenSpec 初始化（proposal + 核心能力规格 + 任务清单）、飞书多维表格看板、缺陷管理工具就位、团队协作约定（T-9、T-10） |

**本文档定稿后，将作为 OpenSpec `proposal` 与 `specs/` 的输入。**

---

## 附 B：文档变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|---|---|---|---|
| v0.1 | 2026-09-17 | AI 起草初版：问题陈述与价值主张、竞品对比、MVP 边界、6 个 Epic／16 个 Feature／42 条用户故事、4 项非功能需求、待确认问题 | AI 起草 + 团队 |
| v0.2 | 2026-09-17 | **R1 需求评审**：修正用户故事优先级统计（19／16／7 → 18／17／7）；补齐 3 条核心故事验收标准；执行 MVP 范围调整 4 项（A-1~A-4）并新增 1 项范围外条目；明确仓库数量边界与预警介入方式；澄清 NFR-1/NFR-2 口径并锁定非功能需求选型；新增第 8 节评审记录、课程要求对齐校核、待办清单与 Sprint 映射 | 团队（R1 评审） |
